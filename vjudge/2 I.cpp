#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,q,k,l;
    cin>>t;
    while(t--)
    {
        vector<int>v;

        cin>>n>>q;
        for(int i=0;i<n;i++)
        {
            cin>>l;
            v.push_back(l);
        }
    }
}
